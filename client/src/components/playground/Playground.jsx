import CoreqWarningModal from "../discovery/CoReqWarningModal";

export const Playground = () => {
  return (
    <div>
      <CoreqWarningModal
        isOpen={true}
        onClose={() => {}}
      />
    </div>
  );
};
