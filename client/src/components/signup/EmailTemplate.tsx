import React from 'react'

function EmailTemplate({firstName, lastName, email, role}: {firstName: string, lastName: string, email: string, role: string}) {
  return (
    <Email title="Onboarding approval">

    </Email>
  )
}

export default EmailTemplate